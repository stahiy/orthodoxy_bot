# Православный Telegram Бот

Бот для уведомления о православных праздниках, чтения молитв и цитат из Библии.

## Возможности

*   📅 **Православный календарь**:
    *   Автоматический расчет переходящих праздников (Пасха, Троица, Великий пост и др.) на основе Александрийской пасхалии.
    *   Учет неподвижных праздников по новому стилю.
    *   Ежедневные утренние уведомления подписчиков о праздниках.
*   🙏 **Молитвослов**: Вывод текстов основных молитв.
*   📖 **Библия**: Вывод цитат из Священного Писания.
*   📿 **Цитаты святых**: Вывод цитат от православных святых отцов.

## Установка и Запуск

### Требования
*   PHP 8.4+
*   Composer

### Установка
1.  Склонируйте репозиторий.
2.  Установите зависимости:
    ```bash
    composer install
    ```
3.  Настройте конфигурацию в `config.php`:
    *   Укажите токен вашего бота (`bot.token`).
    *   При необходимости отредактируйте списки праздников, молитв и цитат.

### Запуск бота (Polling)
Для тестирования и локальной работы запустите бота в режиме опроса:
```bash
php bot.php
```

### Настройка автоматической рассылки (Cron)
Для отправки утренних уведомлений добавьте скрипт `cron.php` в планировщик задач (например, на 9:00 утра):

```bash
0 9 * * * /usr/bin/php /путь/к/проекту/cron.php >> /путь/к/проекту/cron.log 2>&1
```

#### Флаги для cron.php

Скрипт `cron.php` поддерживает флаги для управления типом рассылки:

**По умолчанию (без флагов):**
- Отправляет праздник (если есть) + цитату из Библии

```bash
php cron.php
```

**Только цитата из Библии:**
```bash
php cron.php --bible
# или короткий вариант
php cron.php -b
```

**Только цитата святого:**
```bash
php cron.php --saint
# или короткий вариант
php cron.php -s
```

**Оба типа цитат:**
```bash
php cron.php --bible --saint
# или короткий вариант
php cron.php -b -s
```

**Примеры настройки Cron:**

```bash
# Ежедневно в 9:00 - праздник + цитата из Библии
0 9 * * * /usr/bin/php /путь/к/проекту/cron.php >> /путь/к/проекту/cron.log 2>&1

# Ежедневно в 12:00 - только цитата из Библии
0 12 * * * /usr/bin/php /путь/к/проекту/cron.php --bible >> /путь/к/проекту/cron.log 2>&1

# Ежедневно в 18:00 - только цитата святого
0 18 * * * /usr/bin/php /путь/к/проекту/cron.php --saint >> /путь/к/проекту/cron.log 2>&1
```

## Деплой на хостинг (Timeweb и другие)

### Подготовка к деплою

1. **Загрузите файлы на хостинг** через FTP/SFTP или файловый менеджер панели управления
2. **Установите зависимости** через Composer:
   ```bash
   composer install --no-dev --optimize-autoloader
   ```
3. **Настройте права доступа**:
   ```bash
   chmod 755 storage/
   chmod 644 storage/subscribers.json
   ```

### Настройка Webhook (для работы на хостинге)

На хостинге бот должен работать через webhook, а не polling.

1. **Убедитесь, что у вас есть SSL-сертификат** (HTTPS обязателен для webhook)
2. **Установите webhook** одним из способов:

   **Способ 1: Через браузер**
   - Откройте: `https://ваш-домен.ru/set_webhook.php`
   - Проверьте результат установки

   **Способ 2: Через командную строку**
   ```bash
   php set_webhook.php
   ```

   **Способ 3: Вручную через API Telegram**
   ```bash
   curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://ваш-домен.ru/webhook.php"
   ```

3. **Проверьте установку webhook**:
   ```bash
   curl "https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo"
   ```

4. **После успешной установки** рекомендуется:
   - Удалить файл `set_webhook.php` или защитить его через `.htaccess`
   - Убедиться, что файл `webhook.php` доступен по HTTPS

### Настройка Cron на хостинге

В панели управления хостингом (Timeweb и другие):

1. Откройте раздел **"Cron-задачи"** или **"Планировщик задач"**
2. Создайте новую задачу:
   - **Команда**: `/usr/bin/php /полный/путь/к/проекту/cron.php`
   - **Расписание**: `0 9 * * *` (каждый день в 9:00)
   - **Логирование** (опционально): `>> /полный/путь/к/проекту/cron.log 2>&1`

   Пример для Timeweb:
   ```
   0 9 * * * /usr/bin/php /domains/ваш-домен.ru/public_html/cron.php >> /domains/ваш-домен.ru/public_html/cron.log 2>&1
   ```

### Структура файлов для хостинга

```
public_html/
├── .htaccess              # Защита конфигурации
├── bot.php                # Для локального тестирования (polling)
├── webhook.php            # Основной endpoint для Telegram webhook
├── set_webhook.php        # Скрипт установки webhook (можно удалить после использования)
├── cron.php               # Скрипт для ежедневной рассылки
├── config.php             # Конфигурация (защищен через .htaccess)
├── composer.json
├── composer.lock
├── src/                   # Исходный код приложения
│   ├── Controller/
│   ├── Model/
│   └── View/
├── storage/               # Хранилище данных (защищено через .htaccess)
│   └── subscribers.json
└── vendor/                # Зависимости Composer
```

### Требования хостинга

- **PHP 8.2+** (рекомендуется 8.4)
- **SSL-сертификат** (обязательно для webhook)
- **Доступ к Cron** (для автоматической рассылки)
- **Расширение PHP**: `ext-json` (обычно включено по умолчанию)

### Проверка работы после деплоя

1. Отправьте боту команду `/start` в Telegram
2. Проверьте логи cron (если настроено логирование)
3. Убедитесь, что webhook установлен корректно:
   ```bash
   curl "https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo"
   ```

### Безопасность

- Файл `config.php` защищен через `.htaccess` от прямого доступа
- Папка `storage/` защищена от прямого доступа
- После установки webhook рекомендуется удалить или защитить `set_webhook.php`

## Структура проекта (MVC)

*   **`config.php`** — Конфигурация: токен, пути, базы данных текстов (молитвы, цитаты, праздники).
*   **`bot.php`** — Точка входа. Инициализация и маршрутизация команд.
*   **`cron.php`** — Скрипт для запуска по расписанию (рассылка).
*   **`src/`**:
    *   **`Model/`**:
        *   `CalendarModel.php` — Логика календаря (расчет Пасхалии, объединение праздников).
        *   `ContentModel.php` — Провайдер контента (молитвы, цитаты).
        *   `SubscriberModel.php` — Управление файлом подписчиков (`storage/subscribers.json`).
    *   **`View/`**:
        *   `BotView.php` — Форматирование и отправка сообщений в Telegram.
    *   **`Controller/`**:
        *   `BotController.php` — Обработка команд пользователя (`/start`, `/holiday` и т.д.).

## Источники данных

*   **Праздники**: Рассчитываются алгоритмически (`src/Model/CalendarModel.php`) + список фиксированных дат в `config.php`.
*   **Цитаты и Молитвы**: Хранятся локально в массивах файла `config.php`. Вы можете легко добавить новые, просто отредактировав этот файл.

Мы можем добавить больше цитат прямо в этот массив в config.php.
Мы можем подключить Bible API (например, bible-api.com), чтобы получать случайные стихи на лету.
Мы можем загрузить JSON-файл с полной Библией или набором стихов, если хотите работать автономно, но с большим объемом данных.

0 9 * * * /usr/bin/php /var/www/html/cron.php >> /var/www/html/cron.log 2>&1


кратко набросал.  
Основные:
 - Автоматически вычислять, какой сегодня праздник.
 - Раз в день выводить сообщение об этом.

Советы:
 - Дополнительно, один или более раз в день, выводить совет от святых отцов. Можно привязывать его к празднику, дню недели или другим факторам. Также можно ориентироваться на имя конечного   пользователя или его местоположение.

 Дополнительные функции:
 - Команды для вывода молитв (как минимум основных: «Символ веры», «Отче наш»).
 - К праздникам — выводить краткий ликбез о событии (история праздника, что можно делать есть пить.).
 - Команда для вывода отрывков из Библии — как автоматически (по событию или как напоминание), так и по запросу пользователя.
 - По аналогии с предыдущим пунктом — вывод молитв. Можно в связке с праздниками. Например, в день какого-либо святого выводить в течение дня молитву ему.

Примечание:
Православные часто ищут в сети расписание служб в храмах и информацию о храмах поблизости.

Основная проблема в наполнении БД. 
Отталкиваясь от неё можно используя АПИ распространить и на телегу, Макс, соц сети, виджет.
Добавить общее АПИ